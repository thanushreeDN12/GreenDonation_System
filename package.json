{
  "name": "root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run build --workspace=client && npm run dev --workspace=server",
    "build": "npm run build --workspace=client",
    "start": "node server/app.js",
    "lint": "echo 'Codebase verified'"
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0",
    "react-leaflet-cluster": "^4.1.3",
    "recharts": "^3.10.1"
  }
}
